# PRISM_Engine
A modular system-analysis framework. It runs several mathematical models on the same data, compares what each model reveals, and identifies the stable signals that persist across methods. This creates a model-agnostic view of system structure.
